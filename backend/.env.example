# Database
DATABASE_URL=postgresql://taskuser:taskpass@localhost:5432/ai_task_manager

# Security - IMPORTANT: Generate a secure key using generate_secret_key.py
# Never use the default or example values in production!
SECRET_KEY=

# OpenAI API (required for AI features)
OPENAI_API_KEY=

# CORS
ALLOWED_ORIGINS=http://localhost:3000

# Token expiration in minutes (default: 43200 = 30 days)
# Common values: 10080 (7 days), 43200 (30 days), 129600 (90 days)
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# Email settings for password reset
# Example for Gmail (you'll need an app-specific password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=AI Task Manager
SMTP_TLS=true
SMTP_SSL=false

# Frontend URL for password reset links
FRONTEND_URL=http://localhost:3000

# Password reset token expiry in hours (default: 24)
PASSWORD_RESET_TOKEN_EXPIRE_HOURS=24
